<Window x:Class="ReportManager.Client.Views.ColumnPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ReportManager.Client.ViewModels"
        mc:Ignorable="d"
        Title="Select column"
        Width="520"
        Height="620"
        MinWidth="420"
        MinHeight="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Search" FontWeight="SemiBold"/>
            <TextBox Margin="0,6,0,10"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Type to filter columns and categories"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="6">
            <TreeView ItemsSource="{Binding RootNodes}"
                      SelectedItemChanged="TreeView_OnSelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type vm:ColumnPickerNodeViewModel}" ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding DisplayText}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Setter Property="Opacity" Value="1"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelectable}" Value="False">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Opacity" Value="0.85"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" MinWidth="100" Margin="0,0,8,0" IsCancel="True" Click="CancelButton_OnClick"/>
            <Button Content="Select" MinWidth="100" Margin="0,0,8,0" IsDefault="True" IsEnabled="{Binding CanConfirm}" Click="SelectButton_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
