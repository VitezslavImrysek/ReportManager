<UserControl x:Class="ReportAdmin.App.Views.ColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReportAdmin.App.Views"
             xmlns:vm="clr-namespace:ReportAdmin.App.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:ColumnViewModel}"
             d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="14" FontWeight="SemiBold" Text="Column details"/>

        <!--<TextBlock Grid.Row="1" Margin="0,8,0,0" Foreground="#666"
                   Text="Select a column in the grid above to edit its details.">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasColumn}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>-->

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Key:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,0"
                     Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Margin="16,0,0,0" Text="Type:" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="0" Grid.Column="3" Margin="8,0,0,0"
                      ItemsSource="{Binding ColumnTypeValues}"
                      SelectedItem="{Binding Type, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" Text="Flags:" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="8,8,0,0" Orientation="Horizontal">
                <CheckBox Content="AlwaysSelect" IsChecked="{Binding AlwaysSelect}"/>
                <CheckBox Margin="12,0,0,0" Content="Hidden" IsChecked="{Binding Hidden}"/>
                <CheckBox Margin="12,0,0,0" Content="PrimaryKey" IsChecked="{Binding PrimaryKey}"/>
                <CheckBox Margin="12,0,0,0" Content="Virtual" IsChecked="{Binding Virtual}"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,0,0" Text="Filter:" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="8,8,0,0" Orientation="Horizontal">
                <CheckBox Content="Enabled" IsChecked="{Binding Filterable}"/>
                <CheckBox Margin="12,0,0,0" Content="HasLookup" IsChecked="{Binding HasLookup}" Visibility="{Binding Filterable, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <CheckBox Margin="12,0,0,0" Content="Hidden" IsChecked="{Binding Filter.Hidden}" Visibility="{Binding Filterable, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>

            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,0,0" Text="Sort:" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Margin="8,8,0,0" Orientation="Horizontal">
                <CheckBox Content="Enabled" IsChecked="{Binding Sortable}"/>
                <CheckBox Margin="12,0,0,0" Content="Hidden" IsChecked="{Binding Sort.Hidden}" Visibility="{Binding Sortable, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>

            <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Margin="0,8,0,0" Visibility="{Binding HasLookup, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock FontWeight="SemiBold" Text="SQL lookup"/>
                <TextBlock Margin="0,8,0,0" Text="CommandText:"/>
                <TextBox AcceptsReturn="True" MinLines="4" Text="{Binding Filter.Lookup.Sql.CommandText, UpdateSourceTrigger=PropertyChanged}"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock VerticalAlignment="Center" Text="KeyColumn:"/>
                    <TextBox Margin="8,0,0,0" Width="160" Text="{Binding Filter.Lookup.Sql.KeyColumn, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Margin="16,0,0,0" VerticalAlignment="Center" Text="TextColumn:"/>
                    <TextBox Margin="8,0,0,0" Width="160" Text="{Binding Filter.Lookup.Sql.TextColumn, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
