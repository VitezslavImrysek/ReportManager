<UserControl x:Class="ReportAdmin.App.Views.PresetsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReportAdmin.App.Views"
             xmlns:vm="clr-namespace:ReportAdmin.App.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:PresetsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <Button Padding="10,4" Content="+ Add preset" Command="{Binding AddPresetCommand}"/>
                <Button Margin="8,0,0,0" Padding="10,4" Content="- Remove" Command="{Binding RemovePresetCommand}"/>
            </StackPanel>

            <ListBox Margin="0,10,0,0" 
                     ItemsSource="{Binding SystemPresets}"
                     SelectedItem="{Binding SelectedPreset}"
                     DisplayMemberPath="Name"/>
        </StackPanel>

        <local:PresetView Grid.Column="1" Margin="12,0,0,0" DataContext="{Binding PresetVM}" />
    </Grid>
</UserControl>
